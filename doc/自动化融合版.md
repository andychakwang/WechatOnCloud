# 自动化融合版

> 本文描述 `andy/automation-lab` 分支新增的自动化实验能力。
> 线上 NAS 当前仍建议保持旧版不变；本分支先用于构建、测试和小范围验证。

## 目标

把 WechatOnCloud 的云微信执行端，与本地 `wecom-ai-assistant` 已验证过的业务逻辑融合：

- AI 回复：规则优先，未命中时生成 AI 草稿；发送前必须人工确认。
- 消息导入：可读取实例剪贴板，或先模拟 Ctrl+C 再读取远程微信里的选中文本。
- 规则沉淀：把当前 AI 草稿一键保存为待审核/已审核关键词规则。
- 企微接入资料：导入企业微信自动化 Mac 版导出的 FAQ、话术、人群分组和运营素材。
- 素材资产台账：把 `imageKey/materialKey`、标题、类型、标签、本机路径或 URL 独立登记，云端负责审核和治理，Mac 端负责按 key 映射真实文件。
- 受众资产：把联系人、客户群、群聊、标签和来源沉淀成长期对象，可一键填入群发队列。
- 群发队列：白名单/群聊名单逐条推进，可排期到具体时间，到期后再由 Web、Mac Bridge 或 RPA 包领取下一条。
- 朋友圈半自动：生成/保存/审核朋友圈草稿，可排期运营节奏；到期后由 Mac Bridge 领取并准备到本机剪贴板或当前输入框，不自动点击发布。
- 自动化资产包：规则、资料、受众、素材、群发队列和朋友圈草稿可以导出/预览/导入，便于测试版、开发版和 NAS 间迁移。
- 自动化预检：在 Runner 领取前检查总开关、Mac 心跳、队列开关、规则完整性、素材 key 和本机路径。
- Bridge 自动规划回复：企微 Mac 工具推送消息时可带 `planReplies=true`，云端自动把规则命中或 AI 草稿保存成待审回复；默认不批准 AI 草稿。
- 风险拦截：投诉、退款、验证码、账号、法律等高风险内容直接阻断。
- 冷却与限流：限制每小时发送量和单会话冷却时间，实例发送、Bridge 自动回复和群发都计入限额。
- 运营安全闸：可配置每日自动化交付上限和安静时段；触发后 Web、Bridge 和 RPA 包都会暂停领取回复、群发和朋友圈任务。
- 自动化健康度：把总览、预检和下一步队列聚合成 0-100 分状态，按 AI 回复、群发、朋友圈、Mac Runner、素材受众五条线给出阻断原因和下一步动作。
- 实例感知快照：管理员可对运行中的实例读取 X11 活动窗口、焦点窗口、可见窗口、指针位置、截图能力和可选截图，为后续 OCR/视觉目标校验打底。
- Worker 管控：云端可暂停/恢复指定 Mac Bridge worker；被暂停的 worker 即使继续心跳，也不能拉取队列、领取任务或回写成功状态；Web/CLI 导出的 RPA 包也会按所选 worker 的 `reply`、`mass`、`moment` 能力裁剪任务。
- Bridge 运行回执：Mac Runner/RPA 包上报 run-report 后，云端可把明确的 sent/delivered/prepared/failed 回执反写到 AI 回复、群发和朋友圈队列。
- RPA 包签发台账：Web 导出的运行包会记录签发人、worker、包摘要、任务摘要、有效期和回执状态；Mac 端回传 run-report 后可按摘要关联到对应包。
- 审计记录：记录谁、何时、向哪个实例触发了发送、填入或队列变更动作。

## 安全默认值

这不是一个默认全自动群发器。第一版刻意保守：

- 自动化总开关默认关闭。
- 每条规则默认 `approved=false`。
- 真正发送必须传 `confirm=true`。
- 未审核规则不能发送。
- 风险命中 `block` 时不能发送。
- AI 草稿只生成文本，不自动发送。
- Bridge 自动规划会写入待审回复草稿；只有显式传 `approveRuleReplies=true` 且命中已审核关键词规则时，才会把规则回复批准给 Mac Runner 拉取，AI 草稿不会自动批准。
- Bridge 回复进入 `send` 模式前会再次检查风险词、每小时上限和单会话冷却；`pull-replies --mode send` 只返回当前可发送的回复。
- 安静时段生效时，Bridge/RPA 不会领取 AI 回复、群发或朋友圈任务；Web 手动发送/准备也会被同一套 gate 拦截。
- 每日自动化交付上限默认 `0`，表示不启用；启用后会统计规则发送、文本发送、Bridge 回复送达、群发发送和朋友圈准备/发布审计。
- 暂停的 Mac Bridge worker 只能继续上报心跳/失败/释放领取，不能领取新任务，不能导出包含待办任务的 RPA 包，也不能把任务标记为 delivered/sent/prepared/published。
- 企微接入资料默认需要审核后才会参与 AI 回复和朋友圈文案。
- 群发队列默认未审核，且每次只发送一个目标。
- 未到计划时间的群发和朋友圈草稿不会被 Bridge/RPA 拉取，也不能从 Web 误触发执行。
- 群发自动搜索只使用官方客户端 UI 快捷键，不读取微信数据库、不调用私有协议。
- 朋友圈草稿只复制或填入正文，Bridge 只回写 `prepared/published` 状态，不负责选择图片或点击发布。
- 资产包导入默认会把群发队列和朋友圈草稿重置为未审核草稿，避免导入后被 Mac Runner 立刻领取执行。
- 自动化预检只读当前配置和队列，不会触发领取、发送或发布。

## 新增持久化文件

自动化配置与审计记录独立保存：

```text
/data/automation.json
```

Compose 中已通过 `./data-panel:/data` 持久化，因此它会落到宿主的 `data-panel/automation.json`。

## 环境变量

AI 草稿使用 OpenAI-compatible Chat Completions：

```env
AUTOMATION_AI_API_KEY=
AUTOMATION_AI_BASE_URL=https://api.openai.com/v1
AUTOMATION_AI_MODEL=gpt-4o-mini
```

也兼容 `OPENAI_API_KEY`、`OPENAI_BASE_URL`、`OPENAI_MODEL`。

企微 Mac 工具外部推送资料使用 Bridge token，默认关闭：

```env
AUTOMATION_BRIDGE_TOKEN=
```

填入至少 16 位随机 token 后，Mac 工具可以通过 Bridge API 把 FAQ、话术、联系人分组、素材台账和新消息事件推送到面板。token 只放在环境变量或 NAS Compose 配置里，不写入 GitHub。

## 测试端口部署

自动化版建议先和生产面板并行跑，不直接替换 `36080` 上的稳定服务。本分支提供独立 compose：

```bash
AUTOMATION_AI_API_KEY=你的模型Key \
./scripts/deploy-automation-test.sh
```

脚本默认使用：

- `WOC_IMAGE_PREFIX=ghcr.io/andychakwang`
- `WOC_VERSION=andy-automation-usable-r83-2026-07-06`
- `WOC_TEST_HTTP_PORT=36081`
- `PANEL_ALLOWED_HOSTS=nasbot.cloud`

也可以手动指定：

```bash
WOC_IMAGE_PREFIX=ghcr.io/andychakwang \
WOC_VERSION=andy-automation-usable-r83-2026-07-06 \
WOC_TEST_HTTP_PORT=36081 \
PANEL_ALLOWED_HOSTS=nasbot.cloud \
AUTOMATION_AI_API_KEY=你的模型Key \
AUTOMATION_AI_BASE_URL=https://api.openai.com/v1 \
AUTOMATION_AI_MODEL=gpt-4o-mini \
docker compose -f docker-compose.automation-test.yml up -d
```

隔离项：

- 测试面板容器名：`woc-panel-automation-test`
- 测试数据目录：`./data-panel-automation-test`
- 默认测试端口：`36081`
- 生产面板 `woc-panel`、`./data-panel` 和 `36080` 不受影响

飞牛 NAS 的 Docker 面板可以直接导入固定版 compose，见：

```text
fnos/woc-automation-test/docker-compose.yaml
```

详细步骤见 [飞牛 NAS 自动化测试部署](./飞牛NAS自动化测试部署.md)。

首次进入测试面板后：

1. 新建一个测试微信实例并扫码登录。
2. 在「自动化工作台」选择该实例，先跑「实例自检」。
3. 配置 AI Key 后，用「读取选中文本」验证 AI 回复链路。
4. 创建 1-2 个测试群/联系人队列，先用测试内容跑「发下一条」。
5. 朋友圈先用「复制」模式验证，再尝试「填入」模式。

自动化工作台顶部有健康度、运营总览、下一步队列和预检结果：总开关、已审核资料、AI 回复待办、群发待发送目标、朋友圈待准备草稿、Mac Bridge 在线 worker、出箱待办、阻断项和推荐处理顺序会统一显示。健康度来自 `/api/admin/automation/health`，总览来自 `/api/admin/automation/overview`，下一步队列来自 `/api/admin/automation/action-queue`，预检来自 `/api/admin/automation/preflight`；这些接口都只读当前 JSON store，不会触发发送、领取或发布。健康度会把 AI 回复、群发、朋友圈、Mac Runner、素材受众拆成五条 lane，给出分数、阻断/提醒和下一步动作。下一步队列会按当前完整待办统计可交给 Mac/RPA 的回复、群发和朋友圈任务，并通过 `handoff.rpa` 给出推荐 RPA 包目标、建议导出上限和预检阻断提示，Web 端可一键生成对应目标的 RPA 运行包预览。

## API

所有自动化管理接口都要求管理员登录。

### 自动化健康度

```http
GET /api/admin/automation/health
```

返回 SaaS 工作台的只读健康度：`score` 为 0-100，`level` 为 `ok`、`attention` 或 `blocked`；`lanes` 会按 AI 回复、群发队列、朋友圈、Mac Runner、素材受众分别返回当前分数、待办数、阻断项、提醒项和下一步动作。

```json
{
  "health": {
    "generatedAt": "2026-07-06T00:00:00.000Z",
    "score": 82,
    "level": "attention",
    "summary": "健康度 82，链路可继续推进，但有 2 个提醒；建议先看「Mac Bridge 暂时离线」。",
    "totals": {
      "pendingReplies": 0,
      "pendingMassTasks": 3,
      "pendingMomentTasks": 1,
      "actionItems": 4,
      "workersOnline": 0,
      "workersTotal": 1,
      "preflightBlocks": 0,
      "preflightWarnings": 2
    },
    "lanes": [
      {
        "key": "mass",
        "title": "群发队列",
        "level": "attention",
        "score": 90,
        "ready": true,
        "pending": 3,
        "nextAction": "需要自动处理时启动 Mac Runner。"
      }
    ],
    "recommendedActions": ["需要自动处理时启动 Mac Runner。"]
  }
}
```

### 自动化预检

```http
GET /api/admin/automation/preflight
```

返回当前自动化链路的只读体检报告，前端工作台顶部会展示同一份数据。

```json
{
  "report": {
    "generatedAt": "2026-07-05T00:00:00.000Z",
    "level": "block",
    "summary": { "ok": 2, "warn": 1, "block": 1 },
    "checks": [
      {
        "id": "material_missing",
        "level": "block",
        "title": "素材引用不存在",
        "message": "1 个 imageKey/materialKey 找不到对应素材资产。",
        "refs": ["规则「欢迎语」: demo-poster"]
      }
    ]
  }
}
```

当前预检覆盖：

- 自动化总开关、AI 草稿、规则自动回复、群发和朋友圈开关。
- 安静时段是否正在生效，以及今日自动化交付是否已达每日上限。
- Mac Bridge worker 是否在线且未暂停，以及 Runner 目标是否覆盖待办队列。
- 已审核规则是否有触发词和可执行回复步骤。
- `imageKey/materialKey` 是否存在、启用、审核、类型匹配，并配置了 Mac 可访问的 `localPath`。
- 群发和朋友圈内容是否会被当前风控规则拦截。
- 群发目标、朋友圈草稿和近期 Runner 是否有失败记录。

### 下一步队列

```http
GET /api/admin/automation/action-queue?limit=20
```

返回 SaaS 工作台用的只读待处理动作队列。它会把预检阻断、AI 回复、受控群发目标、朋友圈半自动草稿和最近 Runner 失败聚合成统一 `items`，并按 `block`、`high`、`normal`、`low` 排序。接口只生成推荐动作，不会领取 Bridge 任务，也不会粘贴、发送或发布。`items` 受 `limit` 限制用于页面展示；`handoff.rpa` 会扫描最多 200 个可交付任务，返回完整的回复/群发/朋友圈分布、推荐导出目标和建议 `limit`，避免 Web 端从当前页展示项里误判 RPA 包范围。`handoff.rpa.workerReadiness` 还会按 `replies`、`mass`、`moments` 返回每类待办的在线 worker 数、可处理 worker 数、未知能力 worker 数和是否因为能力缺口被阻断，Web 端可直接展示“为什么现在不能交给某类 Mac Runner”。

```json
{
  "queue": {
    "generatedAt": "2026-07-06T00:00:00.000Z",
    "summary": { "block": 1, "high": 2, "normal": 3, "low": 0, "total": 6 },
    "handoff": {
      "rpa": {
        "target": "all",
        "label": "全队列",
        "total": 18,
        "replies": 10,
        "mass": 6,
        "moments": 2,
        "limit": 50,
        "ready": false,
        "blockedByPreflight": true,
        "reason": "存在预检阻断；可以先导出预览包排查，但执行前应处理阻断项。"
      }
    },
    "items": []
  }
}
```

### 实例自动化自检

```http
POST /api/admin/instances/:id/automation/self-test
```

检查当前实例的自动化基础环境：`DISPLAY`、`xclip`、`xdotool`、活动窗口和剪贴板回环。建议在第一次跑 AI 回复、群发队列或朋友圈草稿前先点管理页里的「实例自检」。

### 实例感知快照

```http
POST /api/admin/instances/:id/automation/inspect
Content-Type: application/json

{
  "includeScreenshot": true
}
```

只读读取实例内 X11 状态，返回 `activeWindow`、`focusedWindow`、`visibleWindows`、`pointer`、`capabilities`、`warnings` 和 `summary`；`includeScreenshot=true` 时会尽力用实例内 `import` 或 `xwd + convert` 生成一张 PNG data URL，并限制截图大小。该接口不会领取任务、不会读取微信数据库、不会粘贴或发送，主要用于上线前确认当前实例聚焦窗口，以及给后续 OCR/视觉识别目标校验预留稳定 API。

### 运营总览

```http
GET /api/admin/automation/overview
```

返回自动化工作台顶部的只读汇总：资料/规则数量、Bridge 消息待办、Mac worker 在线数、群发队列状态、朋友圈草稿状态和最近审计事件。它只读取当前 store，不会领取 Bridge 任务，也不会执行发送。

### 读取配置

```http
GET /api/admin/automation/config
```

### 保存配置

```http
PUT /api/admin/automation/config
Content-Type: application/json

{
  "settings": {
    "enabled": true,
    "aiDraftEnabled": true,
    "automaticRuleRepliesEnabled": true,
    "massSendEnabled": true,
    "momentsEnabled": true,
    "maximumAutomaticSendsPerHour": 20,
    "perConversationCooldownMinutes": 10,
    "requireConfirmForSend": true
  },
  "persona": "你的账号人设和回复风格",
  "knowledgeNotes": "业务资料、报价边界、禁答规则",
  "knowledgeItems": [
    {
      "id": "wecom-faq-001",
      "title": "适合人群",
      "category": "faq",
      "enabled": true,
      "approved": true,
      "source": "wecom-mac",
      "tags": ["咨询", "适合人群"],
      "triggers": ["适合什么人", "适合我吗"],
      "content": "适合已经有私域沟通、需要把常见咨询回复沉淀成半自动流程的团队。",
      "targetNames": [],
      "createdAt": "2026-06-17T00:00:00.000Z",
      "updatedAt": "2026-06-17T00:00:00.000Z"
    }
  ],
  "rules": [
    {
      "id": "welcome-001",
      "name": "咨询欢迎语",
      "enabled": true,
      "approved": true,
      "priority": 10,
      "triggers": ["怎么了解", "咨询", "价格"],
      "responseSteps": [
        { "type": "text", "text": "你好，我先把资料发你，你可以先看一下。", "sendEnter": true },
        { "type": "wait", "seconds": 2 },
        { "type": "text", "text": "如果你方便，也可以直接说下你的需求，我帮你匹配。", "sendEnter": true }
      ],
      "createdAt": "2026-06-17T00:00:00.000Z",
      "updatedAt": "2026-06-17T00:00:00.000Z"
    }
  ]
}
```

### 企微接入资料

导入企业微信自动化 Mac 版导出的资料：

```http
POST /api/admin/automation/knowledge/import
Content-Type: application/json

{
  "source": "wecom-mac",
  "category": "faq",
  "approveImported": false,
  "mode": "upsert",
  "items": [
    {
      "title": "价格咨询边界",
      "category": "policy",
      "tags": ["价格", "报价"],
      "triggers": ["多少钱", "费用", "报价"],
      "content": "价格问题先确认客户场景，不在 AI 回复里编造报价。",
      "targetNames": []
    },
    {
      "title": "测试群发白名单",
      "category": "contact-group",
      "targetNames": ["测试客户群", "张三"]
    }
  ]
}
```

也可以把 JSON、Markdown 或纯文本块放在 `rawText` 字段里，后端会按段落拆成资料。`mode=upsert` 会按 `source + title` 更新同名资料，避免重复导入。

更新审核/启用状态：

```http
PATCH /api/admin/automation/knowledge/:itemId
Content-Type: application/json

{ "approved": true, "enabled": true }
```

删除资料：

```http
DELETE /api/admin/automation/knowledge/:itemId
```

只有 `enabled=true` 且 `approved=true` 的资料会被送进 AI prompt；未审核资料只保存在工作台，不影响实际回复。

### 受众资产

导入企业微信自动化 Mac 版导出的联系人、客户群或群聊：

```http
POST /api/admin/automation/audience/import
Content-Type: application/json

{
  "source": "wecom-mac",
  "type": "group",
  "approveImported": false,
  "mode": "upsert",
  "contacts": [
    {
      "name": "测试客户群",
      "aliases": ["测试群"],
      "tags": ["白名单", "活动提醒"],
      "note": "只用于自动化 smoke 和小范围验证"
    }
  ]
}
```

也可以把一行一个联系人/群聊名放在 `rawText` 字段里。`mode=upsert` 会按 `source + name + type` 更新同名受众，避免重复导入。受众本身不会触发发送，只是长期资产；创建群发队列时仍会生成独立的受控队列，并继续走审核、限流和 Bridge 领取流程。

读取、更新、删除：

```http
GET /api/admin/automation/audience?limit=200&query=&tag=
PATCH /api/admin/automation/audience/:contactId
DELETE /api/admin/automation/audience/:contactId
```

### 素材资产台账

素材资产用于把云端稳定 key 和 Mac 本机真实文件解耦。云端只保存元数据，不上传图片、视频或文件二进制；Mac runner 在执行 `replySteps` 或朋友圈任务时，仍通过 `WECOM_MATERIAL_MAP` / `WECOM_MATERIAL_MAP_FILE` 把 key 解析为本机路径。

导入示例：

```http
POST /api/admin/automation/materials/import
Content-Type: application/json

{
  "source": "wecom-mac",
  "kind": "image",
  "approveImported": false,
  "mode": "upsert",
  "assets": [
    {
      "key": "demo-poster",
      "title": "Demo Poster",
      "localPath": "/Users/andy/Pictures/material.png",
      "tags": ["海报", "朋友圈"],
      "description": "用于回复序列和朋友圈草稿的本机素材"
    }
  ]
}
```

也可以用一行一个素材的文本导入：

```text
demo-poster | Demo Poster | /Users/andy/Pictures/material.png | 海报,朋友圈 | 用于 smoke 验证
```

读取、更新、删除：

```http
GET /api/admin/automation/materials?limit=200&query=&tag=
PATCH /api/admin/automation/materials/:assetId
DELETE /api/admin/automation/materials/:assetId
```

素材台账里的 `approved/enabled` 只代表云端资产可用性；真正执行前，Mac 端仍会检查 key 是否映射到存在的本机文件。未映射或路径不存在时，`dry-run` 会显示缺失项，`prepare/send` 会失败回写而不会继续发送。

### 自动化资产包

资产包用于把一套自动化配置从开发版迁移到测试版，或在升级前做结构化备份。Web 工作台可以直接下载 JSON，也可以粘贴 JSON 先 dry-run 预览，再确认导入。

导出：

```http
GET /api/admin/automation/bundle
```

返回结构包含：

```json
{
  "schema": "wechat-on-cloud.automation-bundle",
  "version": 1,
  "summary": {
    "rules": 1,
    "knowledgeItems": 3,
    "audienceContacts": 20,
    "materialAssets": 5,
    "massSendJobs": 2,
    "momentDrafts": 4,
    "bridgeEvents": 0
  },
  "config": {},
  "audienceContacts": [],
  "materialAssets": [],
  "massSendJobs": [],
  "momentDrafts": []
}
```

预览导入：

```http
POST /api/admin/automation/bundle/import
Content-Type: application/json

{
  "dryRun": true,
  "mode": "upsert",
  "includeConfig": true,
  "keepOperationalState": false,
  "bundle": { "schema": "wechat-on-cloud.automation-bundle" }
}
```

确认导入时把 `dryRun` 改为 `false`。`mode=upsert` 会按规则名、资料 `source+title`、受众 `source+type+name`、素材 `source+key`、队列标题和草稿标题更新；`mode=append` 会全部追加。默认 `keepOperationalState=false`，因此导入的群发队列和朋友圈草稿会变成未审核草稿，不会立刻进入 Bridge 出箱。

### Mac Bridge 推送

测试面板设置 `AUTOMATION_BRIDGE_TOKEN` 后，企业微信自动化 Mac 版可以不登录 Web 管理页，直接推送资料：

```http
POST /api/automation/bridge/wecom/import
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
Content-Type: application/json

{
  "source": "wecom-mac",
  "category": "faq",
  "mode": "upsert",
  "approveImported": false,
  "items": [
    {
      "title": "价格咨询边界",
      "tags": ["价格"],
      "triggers": ["多少钱", "报价"],
      "content": "价格问题先确认客户场景，不在 AI 回复里编造报价。"
    }
  ]
}
```

也可以用 `X-Automation-Token: <token>` 请求头。Bridge 只负责导入资料，不执行发送、群发或朋友圈发布动作；导入后的资料仍需要管理员审核后才会影响 AI 回复。

Mac 工具也可以同步联系人、客户群或群聊到受众资产池：

```http
POST /api/automation/bridge/wecom/audience
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
Content-Type: application/json

{
  "source": "wecom-mac",
  "type": "group",
  "mode": "upsert",
  "contacts": [
    { "name": "测试客户群", "tags": ["白名单"] }
  ]
}
```

Mac 工具也可以同步素材资产台账：

```http
POST /api/automation/bridge/wecom/materials
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
Content-Type: application/json

{
  "source": "wecom-mac",
  "kind": "image",
  "mode": "upsert",
  "approveImported": false,
  "assets": [
    {
      "key": "demo-poster",
      "title": "Demo Poster",
      "localPath": "/Users/andy/Pictures/material.png",
      "tags": ["海报"],
      "description": "Mac 本机素材路径，不上传二进制"
    }
  ]
}
```

已审核且启用的素材可以由 Bridge 导出成本机 Runner 可直接读取的素材映射：

```http
GET /api/automation/bridge/wecom/material-map?kind=image
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
```

返回里的 `materialMap.materials` 兼容 Mac handler 的 `WECOM_MATERIAL_MAP_FILE` 格式；`materialMap.skipped` 会列出已审核但缺少 `localPath` 的素材，避免 Runner 到执行时才发现 key 没法落到本机文件。

Mac 工具也可以把捕捉到的新消息推送到收件箱：

```http
POST /api/automation/bridge/wecom/events
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
Content-Type: application/json

{
  "source": "wecom-mac",
  "events": [
    {
      "externalId": "msg-20260705-001",
      "conversationName": "客户A",
      "senderName": "客户A",
      "inboundText": "你们这个服务适合什么样的人？",
      "conversationContext": "最近几轮对话，可选",
      "receivedAt": "2026-07-05T03:40:00.000Z"
    }
  ]
}
```

带 `externalId` 时按 `source + externalId` 去重更新；没有 `externalId` 时按新事件插入。Web 管理页会在「企微消息收件箱」里显示未归档事件，点击「生成回复」会调用云端计划器，把关键词规则或 AI 草稿保存到这条事件上并标记为 `planned`；草稿仍需人工批准后，Mac Runner 才能拉取。

如果希望 Mac 工具推送消息时直接生成待审草稿，可以在 body 或 query 里传：

```json
{
  "planReplies": true,
  "approveRuleReplies": false
}
```

`approveRuleReplies=true` 只会批准命中已审核关键词规则的回复；AI 草稿默认不会自动批准。CLI 等价命令：

```bash
node scripts/wecom-bridge-client.mjs push-events events.json --plan-replies
node scripts/wecom-bridge-client.mjs push-events events.json --plan-replies --approve-rule-replies
```

Mac 工具可以定时上报心跳，让 Web 管理页显示本机 worker 是否在线、最近运行模式、待处理回复数、待群发任务数和待朋友圈任务数：

```http
POST /api/automation/bridge/wecom/heartbeat
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
Content-Type: application/json

{
  "source": "wecom-mac",
  "workerId": "mac-mini-01",
  "mode": "prepare",
  "host": "Mac-mini",
  "pid": 12345,
  "version": "your-client-version",
  "capabilities": [
    "reply",
    "mass",
    "moment",
    "prepare",
    "material-map",
    "target-match",
    "handler-verification"
  ],
  "materialMap": {
    "file": "/Users/andy/.config/wechat-on-cloud/wecom-materials.json",
    "exists": true,
    "ok": true,
    "mapped": 12,
    "materials": 12,
    "skipped": 1,
    "kinds": ["image"],
    "kind": "image",
    "tag": "朋友圈",
    "source": "wecom-mac-export"
  }
}
```

心跳不会领取或发送任何内容；它只刷新 Bridge worker 状态，并返回当前待交付回复数量、待群发任务数量和待朋友圈任务数量。`capabilities` 用来告诉云端这个 Mac Runner 实际能处理哪些队列、运行引擎和校验能力：`reply`、`mass`、`moment`、`rpa-package`、`prepare`、`send`、`target-match`、`handler-verification`、`visual-verification`、`material-map`。`materialMap` 是 R78 新增的可选状态快照：Runner 会读取 `WECOM_MATERIAL_MAP_FILE`，把文件是否存在、是否可解析、已映射数量、跳过数量和 kind/tag/source 过滤条件带回云端。Web 的 Mac Bridge worker 列表会直接显示素材映射 chip，预检会在存在素材引用但 Runner 未上报、文件缺失、解析异常或仍有跳过项时给出提醒。旧版 Runner 没上报能力时会显示“能力未知”，不会直接阻断。云端策略切到 `runnerEngine=rpa-package` 时，预检会优先确认在线 Runner 是否声明 `rpa-package` 能力。

Mac 工具每轮执行结束后可以上报运行报告，让 Web 管理页看到最近 dry-run/prepare/send 的处理量、失败量、耗时和错误摘要：

```http
POST /api/automation/bridge/wecom/run-report
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
Content-Type: application/json

{
  "source": "wecom-mac",
  "workerId": "mac-mini-01",
  "mode": "dry-run",
  "target": "all",
  "status": "completed",
  "startedAt": "2026-07-05T03:40:00.000Z",
  "finishedAt": "2026-07-05T03:40:03.000Z",
  "durationMs": 3000,
  "handledReplies": 2,
  "handledMassTasks": 1,
  "handledMomentTasks": 1,
  "failedReplies": 0,
  "failedMassTasks": 0,
  "failedMomentTasks": 0,
  "summary": "dry-run total=4",
  "packageHandoff": {
    "generatedFrom": "automation-rpa-package",
    "packageTarget": "all",
    "recommendedMode": "dry-run",
    "runnerEngine": "rpa-package",
    "runnerTarget": "all",
    "runnerMode": "dry-run",
    "allowSend": false,
    "requireTargetMatch": true,
    "requireHandlerVerification": true,
    "momentPasteMode": "clipboard-only",
    "preflightLevel": "warn",
    "preflightSummary": { "ok": 5, "warn": 1, "block": 0 },
    "blockedByPreflight": false,
    "notes": ["云端策略要求目标窗口匹配，Mac handler 应在粘贴/发送前校验会话。"]
  },
  "items": [
    {
      "id": "evt_123",
      "target": "reply",
      "name": "张三",
      "action": "dry-run",
      "dryRun": true,
      "verification": {
        "required": true,
        "verified": true,
        "expectedName": "张三",
        "matchedName": "张三",
        "conversationMatched": true,
        "inputReady": true,
        "activeApp": "企业微信",
        "windowTitle": "企业微信 - 张三",
        "ocrText": "张三\n输入框",
        "visualSummary": "窗口标题和 OCR 均命中目标会话",
        "confidence": 0.98,
        "checkedAt": "2026-07-05T03:40:02.000Z"
      }
    }
  ]
}
```

`items` 是可选的结构化执行明细，用于排查某一轮 Runner 具体处理了哪个回复、群发目标或朋友圈草稿。每个 item 可以带 `verification` 校验快照，供 Mac/RPA 工具回传“是否打开了目标会话、输入框是否就绪、窗口标题/OCR/视觉摘要是什么”。服务端会限长并只保存白名单字段，不保存截图原图。`run-approved`、`run-mass`、`run-moments` 在带 `--report-run` 时会自动上报这些明细；服务端最多保存每条报告前 100 个 item，并保留 id、类型、名称、动作、结果、退出码、错误摘要和校验快照等安全字段。

`packageHandoff` 是 RPA 包执行报告的可选交接快照，用于保存这次 Mac 端执行时采用的云端策略、建议模式和预检结果。`run-rpa-package --report-run` 会自动带上导出包里的 handoff；服务端只保留白名单字段和最多 5 条短 notes。Web 管理页的最近运行列表会展示“RPA 包目标 / 建议模式 / 预检等级”，用于 SaaS 运维排查“为什么这轮只 prepare、为什么没有 send、是否因为预检阻断”等问题。

管理员还可以读取最近运行健康汇总，供 SaaS 运维面板展示多 worker 稳定性、RPA 包执行状态和高频失败原因：

```http
GET /api/admin/automation/bridge-runs/summary?hours=24&limit=300&workerId=mac-mini-01
```

`hours` 默认 24，最多 720；`limit` 最多读取最近 300 条报告；`workerId` 可选。返回体会按状态、目标、模式聚合运行次数，并给出处理/失败总量、RPA 包运行数、被预检阻断的 RPA 包次数、校验通过/失败数量、每个 worker 的最近状态和 Top 失败原因。Web 管理页的「Mac Bridge」区域会显示“运行健康”指标条，用来快速判断最近一段时间是没有 Runner 上报、运行稳定，还是存在窗口不匹配、企业微信窗口未找到等需要人工处理的问题。这个接口只读，不会领取任务、不会回写队列、不会触发发送。

旧版 Mac 工具也可以先不改 handler，直接用 `report-run --items-json` 上报明细：

```bash
node scripts/wecom-bridge-client.mjs report-run \
  --worker-id mac-mini-01 \
  --target replies \
  --mode prepare \
  --handled-replies 1 \
  --items-json '[{"id":"evt_123","target":"reply","conversationName":"张三","action":"prepared","ok":true,"verification":{"required":true,"verified":true,"matchedName":"张三","windowTitle":"企业微信 - 张三"}}]'
```

新版 handler 可以在 stdout 输出一个 JSON 对象，Bridge client 会自动解析其中的 `verification`、`targetVerification` 或 `visualCheck` 并挂到运行报告 item 上。示例：

```json
{
  "ok": true,
  "verification": {
    "required": true,
    "verified": true,
    "matchedName": "张三",
    "conversationMatched": true,
    "inputReady": true,
    "windowTitle": "企业微信 - 张三",
    "ocrText": "张三\n输入框"
  }
}
```

如果要把云控队列交给现有“企业微信自动化 Mac 版”项目处理，而不是立刻让 Bridge runner 执行，可以先导出标准 RPA 运行包：

```bash
node scripts/wecom-bridge-client.mjs export-rpa-package \
  --target all \
  --limit 50 \
  --ttl-minutes 720 \
  --format jsonl \
  --output-dir ~/.config/wechat-on-cloud/rpa-packages
```

`export-rpa-package` 是只读命令：它只拉取已批准且待处理的 AI 回复、受控群发目标和朋友圈草稿，不会领取任务、不会粘贴、不会发送。拉取时会带上当前 `workerId/source/capabilities`，因此暂停的 worker 会拿到空包；声明了能力的 worker 只会拿到自己能处理的任务，例如 `capabilities=reply` 只导出 AI 回复，不会导出群发或朋友圈任务；安静时段、每日上限和计划时间也会继续生效。输出包使用 `woc.wecom.rpa.package.v1` / `woc.wecom.rpa.task.v1` schema；每条任务都会带 `target`、`operation`、`expectedName`、`text`、`textChars`、`requiresOperatorReview`，并按类型补充：

- `reply.prepare`：`conversationName`、`senderName`、`inboundText`、`steps`、`imageStepCount`。
- `mass.prepare`：`recipientName`、`jobId`、`itemId`、`jobTitle`。
- `moment.prepare`：`draftId`、`title`、`imageNotes`、`materials`。

包级 `expiresAt` / `ttlMinutes` 默认给 RPA 包 12 小时有效期，`handoff.packageExpiresAt` 和每条 JSONL task 的 `expiresAt` 会保留同一个时间。过期包默认不能执行，避免旧下载包在队列、策略或人工审核已经变化后继续被 Mac 端误跑；排障时才用 `run-rpa-package --allow-expired-rpa-package` 显式绕过。

包级 `packageDigest` / `taskDigest` 使用 SHA-256 记录这次交接包和任务清单的指纹。服务端导出的 JSON 包、JSONL 任务、Mac client 本地生成包、`run-rpa-package --report-run` 上报的 `packageHandoff` 都会保留这些摘要；Web 最近运行列表和 RPA 包签发台账只展示前 12 位，方便把“云端下发包”和“Mac 实际执行包”对起来。

Web 管理端导出 RPA 包时会写入签发台账，记录 `packageId`、`packageDigest`、`taskDigest`、签发人、目标队列、worker、有效期、任务数量和请求类型。Mac/RPA 项目执行包并带 `--report-run` 回传后，云端会优先按 `packageDigest`，再按 `packageId + taskDigest` 关联已有签发记录；如果包来自 Mac 端本地导出而非 Web 下载，则自动补一条 `reported-only` 记录。台账保留最近 300 条，Web 管理页会显示最近签发包的“已签发 / 已回执 / 已过期”状态。

包级 `handoff` 会把云端 Runner 策略和预检状态一起交给 Mac/RPA 项目：`packageId`、`packageTarget`、`packageDigest`、`taskDigest`、`packageTaskCount`、`packageExpiresAt`、`packageTtlMinutes`、`recommendedMode`、`runnerEngine`、`runnerTarget`、`runnerMode`、`allowSend`、`requireTargetMatch`、`requireHandlerVerification`、`momentPasteMode`、`preflightLevel`、`preflightSummary`、`blockedByPreflight` 和可展示给操作者的 `notes`。这样现有“企业微信自动化 Mac 版”消费包时，可以直接知道本轮应 dry-run、prepare 还是 send；如果云端策略是 send 但未显式允许发送，或预检存在阻断项，Mac 端应按 `recommendedMode` 降级处理。朋友圈任务即使混在 all 包里，也仍按半自动 prepare 执行，不做无人值守发布。

Mac 端也可以直接消费这个 JSON/JSONL 包，把任务分发给现有 handler：

```bash
node scripts/wecom-bridge-client.mjs run-rpa-package \
  ~/.config/wechat-on-cloud/rpa-packages/latest.jsonl \
  --target all \
  --mode dry-run \
  --handler-reply ./scripts/wecom-mac-reply-handler.sh \
  --handler-mass ./scripts/wecom-mac-mass-handler.sh \
  --handler-moment ./scripts/wecom-mac-moment-handler.sh
```

`run-rpa-package` 默认只做 `dry-run`，会读取包、校验任务结构和过期时间，并调用三个 handler 的 dry-run 逻辑，不会领取任务、不会回写成功状态，也不会触碰企业微信发送动作。改成 `--mode prepare` 时，回复/群发会准备输入框，朋友圈会复制草稿或按 `WECOM_MOMENT_PASTE_MODE` 进入半自动准备；改成 `--mode send` 时，只允许回复和群发在本机设置 `WECOM_ALLOW_SEND=1` 后真实发送，朋友圈仍会降级为半自动 `prepare`。需要让包执行结果回写云端时再加：

```bash
# 成功后回写 delivered/sent/prepared；失败时也写 failed，并生成一次运行报告
node scripts/wecom-bridge-client.mjs run-rpa-package latest.jsonl \
  --mode prepare \
  --ack \
  --report-failure \
  --report-run
```

这样旧的“企业微信自动化 Mac 版”既可以自己消费包完成窗口定位、输入框校验、素材解析和人工确认，也可以逐步复用本仓库的 handler 与回写能力。中间的 RPA 包是可审计边界：云端负责审核/队列/AI 草稿，本机负责真实企业微信 RPA。

如果不想先落盘再运行，Mac 端可以直接从面板拉取一份标准 RPA 包并执行：

```bash
node scripts/wecom-bridge-client.mjs run-cloud-rpa-package \
  --target all \
  --limit 50 \
  --mode dry-run \
  --save-package-dir ~/.config/wechat-on-cloud/rpa-packages
```

`run-cloud-rpa-package` 内部复用同一套 `woc.wecom.rpa.package.v1` 包格式：先用 Bridge token 拉取待处理回复、群发目标和朋友圈草稿，再把包里的任务分发给 reply/mass/moment handler。它默认仍是安全的 `dry-run`，不会领取任务；`--mode prepare/send` 的执行语义和 `run-rpa-package` 一致，`--ttl-minutes` 会控制本轮包的有效期，`--save-package-dir` 会额外保留本轮拉到的 JSONL 包，方便把这份包交给现有“企业微信自动化 Mac 版”项目排查或二次处理。

Runner 也支持走这个包边界，便于逐步替换旧项目的任务入口：

```bash
WECOM_USE_RPA_PACKAGE=1 \
WECOM_RUNNER_MODE=dry-run \
WECOM_RUNNER_TARGET=all \
scripts/wecom-bridge-runner.sh run-once
```

常驻运行时可在 `~/.config/wechat-on-cloud/wecom-bridge.env` 里加入 `WECOM_USE_RPA_PACKAGE=1` 和 `WECOM_RPA_PACKAGE_SAVE_DIR=$HOME/.config/wechat-on-cloud/rpa-packages`。这个模式会先回传 Mac worker 心跳，再通过统一 RPA 包处理队列；真实 `send` 仍需要本机显式设置 `WECOM_ALLOW_SEND=1`，朋友圈仍只进入半自动准备，不会自动点击发布。

Web 管理页的「Mac Bridge / RPA 运行包」也可以生成同一份包，适合 SaaS 面板里人工下载、交给 Mac 项目执行，或先检查待办数量。对应只读接口：

```http
GET /api/admin/automation/rpa-package?target=all&limit=50&format=json
GET /api/admin/automation/rpa-package?target=all&limit=50&format=jsonl&download=1
GET /api/admin/automation/rpa-package/issues?limit=50
```

`target` 支持 `replies`、`mass`、`moments`、`all`；`limit` 是每类任务的上限；`ttlMinutes` 默认 720，可缩短或延长到 5 分钟至 7 天；`includeSource=1` 会把源任务一起放进包里用于排障。可选传 `workerId`、`workerSource`、`capabilities` 和 `mode=send|prepare|dry-run`，让 Web 导出的包按某台 Mac worker 的状态和能力过滤；worker 被暂停时，返回包仍会生成，但 `tasks` 为空；worker 明确缺少某类能力时，`target=all` 也会跳过对应任务。Web 管理页的 RPA 运行包区域也提供同样的 Worker 选择、有效期设置，预览和 JSON/JSONL 下载都会带上所选 worker 的来源、能力、有效期和当前 Runner 模式，并在下方展示最近签发/回执台账。这个 Web 导出接口和 CLI 导出一样不会 claim、不会回写、不会发送。JSON 格式的响应会包含同一份 `handoff` 元数据，Web 预览会展示建议模式、预检等级、有效至时间和前三条交接提示；`run-rpa-package --report-run` 也会把 `packageHandoff` 带回运行报告，方便 SaaS 面板回看某次 Mac 执行时采用的云端策略。

管理员可以在 Web 管理页维护 Mac Runner 的云端策略，也可以直接调用接口：

```http
GET /api/admin/automation/runner-policy
PUT /api/admin/automation/runner-policy
Content-Type: application/json

{
  "runnerEngine": "bridge",
  "mode": "dry-run",
  "target": "all",
  "limit": 5,
  "claimTtlSeconds": 300,
  "heartbeatIntervalSeconds": 60,
  "momentPasteMode": "clipboard-only",
  "allowSend": false,
  "requireTargetMatch": true,
  "requireHandlerVerification": true
}
```

Bridge worker 可在本机定时任务启动时拉取这份策略：

```http
GET /api/automation/bridge/wecom/runner-policy?workerId=mac-mini-01
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
```

返回体包含 `policy` 和可直接映射成本机环境变量的 `env`。`runnerEngine=bridge` 是默认队列执行方式；`runnerEngine=rpa-package` 会下发 `WECOM_RUNNER_ENGINE=rpa-package` 和 `WECOM_USE_RPA_PACKAGE=1`，Mac runner 每轮会先拉取标准 RPA 包，再按包内任务分发到 reply/mass/moment handler。这个模式适合让现有“企业微信自动化 Mac 版”项目只对接统一包格式，而不直接理解云端多个队列 API。如果云端策略选择 `send`，仍需要本机显式设置 `WECOM_ACCEPT_REMOTE_SEND=1` 或 `WECOM_ALLOW_SEND=1`，否则 runner 会自动降级为 `prepare`。`requireTargetMatch=true` 会下发 `WECOM_REQUIRE_TARGET_MATCH=1`，让回复和群发 handler 在粘贴/发送前硬校验企业微信窗口标题。`requireHandlerVerification=true` 会下发 `WECOM_REQUIRE_HANDLER_VERIFICATION=1`，Bridge client 只有在 handler 回传正向 `verification` 后才会把回复、群发或朋友圈任务标记为成功；校验缺失或失败会进入 failed 并写入运行报告。`WECOM_BRIDGE_CAPABILITIES` 可显式覆盖 worker 心跳和领取回写能力清单；云端会按已声明能力拦截不匹配的回复、群发或朋友圈任务领取/成功回写。朋友圈目标不支持无人值守 `send`；保存策略时会规范为半自动 `prepare`。

管理员可用 `GET /api/admin/automation/bridge-runs?limit=50` 查看最近运行报告；报告会保留最近 300 条，用于 SaaS 运维看板和多 worker 排障。RPA 包执行报告会额外保留 `packageHandoff`，用于回看当时的云端 Runner 策略、建议执行模式和预检状态。

如果 Mac Runner 掉线、中断或任务失败，管理员可以在 Web 管理页预览并恢复 Bridge 出箱，也可以直接调用接口：

```http
POST /api/admin/automation/bridge-recovery
Content-Type: application/json

{
  "releaseClaims": "expired",
  "retryFailed": true,
  "workerId": "mac-mini-01",
  "failureReason": "window not ready",
  "minFailedAgeSeconds": 300,
  "maxRetryAttempts": 3,
  "limit": 100,
  "cursor": "reply|retry-failed|evt_122"
}
```

默认只释放已超时领取，并把失败的 AI 回复、群发目标和朋友圈草稿重新放回待处理队列；不会直接发送内容。管理页支持先 `dryRun` 预览、按回复/群发/朋友圈限定范围、按 worker、失败原因、失败冷却、重试次数上限和 cursor 分批恢复，再执行恢复。带 `workerId` 时，只释放该 worker 的领取，只重试记录为该 worker 失败的任务；历史失败项如果没有失败归属，会在 worker 过滤下跳过。带 `failureReason` 时，只重试错误文本包含该内容的失败项；带 `minFailedAgeSeconds` 时，刚失败且未过冷却时间的任务会暂缓重试；带 `maxRetryAttempts` 时，已达到该恢复次数的失败项会跳过，`0` 表示不限。领取释放不受失败原因、失败冷却和重试上限影响。返回体会按 `replies`、`mass`、`moments` 统计释放和重试数量，并给出本轮变更明细：

```json
{
  "result": {
    "dryRun": false,
    "releaseClaims": "expired",
    "retryFailed": true,
    "workerId": "mac-mini-01",
    "failureReason": "window not ready",
    "minFailedAgeSeconds": 300,
    "maxRetryAttempts": 3,
    "limit": 100,
    "hasMore": true,
    "nextCursor": "reply|retry-failed|evt_123",
    "replies": { "releasedClaims": 0, "retriedFailed": 1 },
    "mass": { "releasedClaims": 0, "retriedFailed": 1, "resumedJobs": 1 },
    "moments": { "releasedClaims": 0, "retriedFailed": 1 },
    "totalChanged": 3,
    "changes": [
      { "target": "reply", "id": "evt_123", "action": "retry-failed", "reason": "failed_retry", "workerId": "mac-mini-01", "error": "window not ready", "failedAt": "2026-07-05T06:30:00.000Z", "retryCount": 1, "nextRetryCount": 2, "cursor": "reply|retry-failed|evt_123" }
    ]
  }
}
```

可选参数：

- `dryRun: true`：只预览，不落库。
- `releaseClaims: "all"`：强制释放全部未交付领取，用于明确要接管某台 worker 的任务。
- `workerId: "mac-mini-01"`：按 worker 精准恢复；领取态匹配 `replyClaimedBy` / `bridgeClaimedBy`，失败态匹配 `replyFailedBy` / `bridgeFailedBy`。
- `failureReason` / `errorContains`：按失败原因文本筛选重试项；只影响失败重试，不影响领取释放。
- `minFailedAgeSeconds` / `failedCooldownSeconds`：失败冷却时间；失败时间未达到该秒数的重试项会跳过，默认 0，最大 7 天。老数据没有失败时间时，如果启用冷却会跳过。
- `maxRetryAttempts` / `maxRetries`：失败恢复次数上限；每次真正把失败项恢复为待处理时加 1，达到上限后跳过，默认 0 表示不限。
- `includeReplies` / `includeMass` / `includeMoments`：按类型限定恢复范围。
- `limit`：限制本轮最多处理的变更项，默认 500，最大 2000。
- `cursor` / `afterCursor`：从上次返回的游标之后继续扫描；当 `hasMore` 为 true 时，用 `nextCursor` 拉取下一批。

管理员在收件箱里保存并批准回复草稿后，Mac 工具可以拉取待执行回复：

```http
GET /api/automation/bridge/wecom/replies?limit=50
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
```

返回的是已批准、未交付、未归档的事件，包含 `replyDraft`、可选 `replySteps`、会话名和原始消息。`replyDraft` 是单段兼容文本；`replySteps` 用于云端顺序回复，当前支持：

```json
[
  { "type": "text", "text": "第一段回复", "sendEnter": true },
  { "type": "wait", "seconds": 3 },
  { "type": "image", "imagePath": "/Users/andy/Pictures/material.png", "sendEnter": true },
  { "type": "image", "imageKey": "demo-poster", "sendEnter": true },
  { "type": "wait", "seconds": 2 },
  { "type": "text", "text": "第二段回复", "sendEnter": true }
]
```

这对应企业微信自动化 Mac 版里的 `KeywordReplyStep`：`contentType=text` 映射为 `text` 步骤，`contentType=image` 映射为 `image` 步骤，`delayBeforeSendingSeconds` 映射为前置 `wait` 步骤。图片可以直接传 Mac runner 本机 `imagePath`，也可以只传云端稳定的 `imageKey` / `materialKey`；后者由 Mac runner 用本机素材映射解析成真实路径，不上传二进制到云端。`prepare/send` 时路径不存在或 key 未映射会失败回写，`dry-run` 会显示 `imageStepCount`、缺失路径和缺失 key。Mac 工具执行前仍应在本机界面做一次人工确认或停在发送前确认。

Web 收件箱里也可以用文本语法保存顺序回复：

```text
第一段回复

[wait 3]

[image /Users/andy/Pictures/material.png]

[image-key demo-poster]

第二段回复
```

Mac runner 的素材映射可以用环境变量直接传：

```bash
export WECOM_MATERIAL_MAP='{"demo-poster":"/Users/andy/Pictures/material.png"}'
```

也可以放到本机 JSON 文件：

```json
{
  "materials": [
    { "key": "demo-poster", "path": "/Users/andy/Pictures/material.png" }
  ]
}
```

然后运行前设置：

```bash
export WECOM_MATERIAL_MAP_FILE=/Users/andy/.config/wecom-materials.json
```

更推荐让 Mac Bridge 直接从云端素材台账生成这份文件：

```bash
node scripts/wecom-bridge-client.mjs material-map --kind image --include-skipped 1 --output ~/.config/wechat-on-cloud/wecom-materials.json
export WECOM_MATERIAL_MAP_FILE=$HOME/.config/wechat-on-cloud/wecom-materials.json
```

`scripts/wecom-bridge-runner.sh` 在检测到 `WECOM_MATERIAL_MAP_FILE` 时会默认刷新云端素材映射；默认同步 `image` 类型并保留 `skipped` 诊断。可用 `WECOM_MATERIAL_MAP_KIND=image|video|file|link|text|other|all`、`WECOM_MATERIAL_MAP_TAG`、`WECOM_MATERIAL_MAP_SOURCE` 和 `WECOM_MATERIAL_MAP_INCLUDE_SKIPPED=0|1` 控制云端素材过滤；如需临时禁用，设置 `WECOM_SYNC_MATERIAL_MAP=0`。R78 起，Runner 在同步后会通过 heartbeat 把本机映射文件状态上报给云端，便于 SaaS 面板判断朋友圈/RPA 选图前的本机素材准备度。

Mac 工具执行前可以先领取，避免多台 Mac 同时处理同一条：

```http
PATCH /api/automation/bridge/wecom/replies/:eventId
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
Content-Type: application/json

{
  "deliveryStatus": "claimed",
  "workerId": "mac-mini-01",
  "claimTtlSeconds": 300
}
```

领取后的回复默认不会再出现在 `GET /replies` 待处理列表里；如果 Mac 工具没有回写成功/失败，领取状态默认 300 秒后过期，届时该回复会重新出现在待处理列表中。Mac 工具处理失败时，可以标记失败并记录原因，方便 Web 面板查看和下次重试：

```http
PATCH /api/automation/bridge/wecom/replies/:eventId
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
Content-Type: application/json

{
  "deliveryStatus": "failed",
  "workerId": "mac-mini-01",
  "error": "企业微信窗口未找到"
}
```

如果只是本机取消处理、窗口没准备好，想立即放回待处理列表，可以释放领取：

```http
PATCH /api/automation/bridge/wecom/replies/:eventId
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
Content-Type: application/json

{
  "deliveryStatus": "released",
  "workerId": "mac-mini-01"
}
```

Mac 工具处理成功后，可以标记已交付：

```http
PATCH /api/automation/bridge/wecom/replies/:eventId
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
Content-Type: application/json

{
  "deliveryStatus": "delivered",
  "workerId": "mac-mini-01"
}
```

为了兼容早期客户端，空 JSON `{}` 仍等价于 `delivered`。

这只会记录 `replyDeliveredAt`，不会由云端直接发送消息。

管理员审核并启动群发队列后，Mac 工具也可以拉取受控群发任务：

```http
GET /api/automation/bridge/wecom/mass-tasks?limit=50
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
```

返回的是已开启自动化总开关、已开启群发开关、已审核且状态为 `queued/running` 的队列任务。每个队列同一时间只暴露第一个 `pending` 目标，保证顺序推进；被有效领取的目标不会再出现在待处理列表中。

```http
PATCH /api/automation/bridge/wecom/mass-tasks/:taskId
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
Content-Type: application/json

{
  "deliveryStatus": "claimed",
  "workerId": "mac-mini-01",
  "claimTtlSeconds": 300
}
```

`deliveryStatus` 可取：

- `claimed`：领取任务，默认 300 秒后超时释放。
- `released`：窗口未准备好时释放领取，立即回到待处理列表。
- `failed`：标记当前目标失败，并暂停队列，Web 管理页可重置或跳过。
- `sent` / `delivered`：标记当前目标已发送，队列继续推进或完成。

这只会更新队列状态；真正打开企业微信、搜索目标、粘贴或发送消息仍发生在 Mac 本机。

管理员审核朋友圈草稿并置为 `ready` 后，Mac 工具可以拉取半自动朋友圈任务：

```http
GET /api/automation/bridge/wecom/moment-tasks?limit=50
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
```

返回的是已开启自动化总开关、已开启朋友圈开关、已审核且状态为 `ready` 的草稿。被有效领取的草稿不会再出现在待处理列表中。

```http
PATCH /api/automation/bridge/wecom/moment-tasks/:taskId
Authorization: Bearer <AUTOMATION_BRIDGE_TOKEN>
Content-Type: application/json

{
  "deliveryStatus": "claimed",
  "workerId": "mac-mini-01",
  "claimTtlSeconds": 300
}
```

`deliveryStatus` 可取：

- `claimed`：领取草稿，默认 300 秒后超时释放。
- `released`：窗口未准备好时释放领取，立即回到待处理列表。
- `failed`：标记准备失败，草稿退回未审核 `draft`，避免反复重试。
- `prepared`：Mac 端已把文案准备到剪贴板或当前输入框，Web 状态变为 `prepared`。
- `published`：人工确认已经发布后回写状态；Bridge 本身不会点击发布。

这只会更新草稿状态；真正打开企业微信、进入朋友圈、粘贴正文和发布确认仍发生在 Mac 本机。

仓库内提供了一个 Mac 侧零依赖客户端，方便嵌入企业微信自动化 Mac 版：

```bash
export WOC_PANEL_URL='http://nasbot.cloud:36081'
export AUTOMATION_BRIDGE_TOKEN='至少16位随机字符串'

# 推送 FAQ / 话术 / 群发名单 / 朋友圈素材
node scripts/wecom-bridge-client.mjs import-knowledge doc/examples/wecom-knowledge.sample.json

# 推送联系人 / 客户群 / 群聊到受众资产池
node scripts/wecom-bridge-client.mjs import-audience doc/examples/wecom-audience.sample.json

# 从已授权的 @wecom/cli 通讯录同步联系人到受众资产池（默认未审核，不会进入群发队列）
node scripts/wecom-bridge-client.mjs sync-cli-audience --source wecom-cli-contact --tag wecom-cli
node scripts/wecom-bridge-client.mjs sync-cli-audience --dry-run --output ~/.config/wechat-on-cloud/wecom-cli-audience.json

# 推送素材 key / 本机路径到素材资产台账
node scripts/wecom-bridge-client.mjs import-materials doc/examples/wecom-materials.sample.json

# 把已审核素材台账同步成本机 handler 可读取的映射文件
node scripts/wecom-bridge-client.mjs material-map --kind image --include-skipped 1 --output ~/.config/wechat-on-cloud/wecom-materials.json

# 推送 Mac 侧捕捉到的新消息
node scripts/wecom-bridge-client.mjs push-events doc/examples/wecom-events.sample.json
node scripts/wecom-bridge-client.mjs push-events doc/examples/wecom-events.sample.json --plan-replies

# 上报当前 Mac worker 心跳，Web 管理页会显示在线状态
node scripts/wecom-bridge-client.mjs heartbeat --worker-id mac-mini-01 --mode prepare

# 上报本轮 runner 执行结果，Web 管理页会显示最近运行历史
node scripts/wecom-bridge-client.mjs report-run --worker-id mac-mini-01 --target all --mode dry-run --handled-replies 2

# 上报带目标会话/OCR 校验快照的运行明细
node scripts/wecom-bridge-client.mjs report-run --worker-id mac-mini-01 --target replies --mode prepare --items-json '[{"id":"evt_123","target":"reply","conversationName":"张三","verification":{"verified":true,"matchedName":"张三","windowTitle":"企业微信 - 张三"}}]'

# 拉取 Web 管理员已批准、尚未交付的回复
node scripts/wecom-bridge-client.mjs pull-replies --limit 20

# 执行前领取，避免多台 Mac 重复处理
node scripts/wecom-bridge-client.mjs claim-reply <eventId> --worker-id mac-mini-01 --claim-ttl-seconds 300

# 取消本机处理，立即放回待处理列表
node scripts/wecom-bridge-client.mjs release-reply <eventId> --worker-id mac-mini-01 --reason '窗口未准备好'

# 本机工具执行失败时回写原因，Web 面板可见并允许下次重试
node scripts/wecom-bridge-client.mjs mark-failed <eventId> --error '企业微信窗口未找到'

# 本机工具执行成功后回写交付状态
node scripts/wecom-bridge-client.mjs mark-delivered <eventId>

# 拉取 Web 管理员已审核、已排队的群发任务
node scripts/wecom-bridge-client.mjs pull-mass-tasks --limit 5

# 群发任务执行前领取 / 释放 / 回写失败 / 回写已发送
node scripts/wecom-bridge-client.mjs claim-mass-task <taskId> --worker-id mac-mini-01 --claim-ttl-seconds 300
node scripts/wecom-bridge-client.mjs release-mass-task <taskId> --worker-id mac-mini-01 --reason '窗口未准备好'
node scripts/wecom-bridge-client.mjs mark-mass-failed <taskId> --error '企业微信窗口未找到'
node scripts/wecom-bridge-client.mjs mark-mass-sent <taskId> --worker-id mac-mini-01

# 拉取 Web 管理员已审核、已就绪的朋友圈任务
node scripts/wecom-bridge-client.mjs pull-moment-tasks --limit 5

# 朋友圈任务执行前领取 / 释放 / 回写失败 / 回写已准备 / 人工发布后回写已发布
node scripts/wecom-bridge-client.mjs claim-moment-task <taskId> --worker-id mac-mini-01 --claim-ttl-seconds 300
node scripts/wecom-bridge-client.mjs release-moment-task <taskId> --worker-id mac-mini-01 --reason '发布框未准备好'
node scripts/wecom-bridge-client.mjs mark-moment-failed <taskId> --error '企业微信窗口未找到'
node scripts/wecom-bridge-client.mjs mark-moment-prepared <taskId> --worker-id mac-mini-01
node scripts/wecom-bridge-client.mjs mark-moment-published <taskId> --worker-id mac-mini-01
```

如果 Mac 项目已经有本地发送脚本，可以用 `run-approved` 串起来：

```bash
node scripts/wecom-bridge-client.mjs run-approved \
  --handler './send-to-wecom.sh' \
  --claim \
  --mark-delivered \
  --report-failure
```

`run-approved` 会把单条回复 JSON 写入 handler 的 stdin，并设置 `WECOM_BRIDGE_REPLY_ID`、`WECOM_BRIDGE_CONVERSATION_NAME`、`WECOM_BRIDGE_SENDER_NAME`、`WECOM_BRIDGE_REPLY_DRAFT`、`WECOM_BRIDGE_REPLY_STEPS`、`WECOM_BRIDGE_REPLY_STEP_COUNT`、`WECOM_BRIDGE_REPLY_IMAGE_STEP_COUNT` 环境变量。仓库自带的 Mac handler 在 prepare/send 成功后会默认读取企业微信前台窗口标题，并把 `verification` 快照写到 stdout，Bridge client 会随运行报告回传到云端；如需临时关闭，设置 `WECOM_VERIFY_TARGET=0`。启用 `WECOM_REQUIRE_TARGET_MATCH=1` 后，handler 会在搜索会话后、粘贴/发送前读取窗口标题；如果未命中目标会话，会以非 0 退出并回传失败校验快照，不会继续写入输入框。启用 `WECOM_REQUIRE_HANDLER_VERIFICATION=1` 后，即使 handler 退出码为 `0`，只要没有回传 `verified/conversationMatched/inputReady` 等正向信号，Bridge client 也不会执行 `--mark-delivered`，而是把任务标记为 failed。只有 handler 退出码为 `0`、交付校验通过，并且显式传了 `--mark-delivered`，客户端才会回写已交付。这样 Mac 侧仍可以停在发送前确认或做本机风控。

`--claim-ttl-seconds` 可控制领取超时时间，默认 300 秒。超时后回复会再次被 `pull-replies`/`run-approved` 拉到；未超时前，其他 worker 不能抢占同一条回复。

群发队列也可以用 `run-mass` 串接本机 handler：

```bash
node scripts/wecom-bridge-client.mjs run-mass \
  --handler './scripts/wecom-mac-mass-handler.sh' \
  --claim \
  --mark-sent \
  --report-failure
```

`run-mass` 会把单条群发任务 JSON 写入 handler 的 stdin，并设置 `WECOM_BRIDGE_MASS_TASK_ID`、`WECOM_BRIDGE_RECIPIENT_NAME`、`WECOM_BRIDGE_MASS_MESSAGE` 等环境变量。自带群发 handler 同样会把窗口标题和目标匹配结果写入 `verification`；启用 `WECOM_REQUIRE_TARGET_MATCH=1` 后会先校验窗口标题是否命中收件人/群名，再决定是否粘贴群发内容。启用 `WECOM_REQUIRE_HANDLER_VERIFICATION=1` 后，Bridge client 会要求 handler 的 `verification` 有正向信号，才会执行 `--mark-sent` 回写已发送。

朋友圈半自动也可以用 `run-moments` 串接本机 handler：

```bash
node scripts/wecom-bridge-client.mjs run-moments \
  --handler './scripts/wecom-mac-moment-handler.sh' \
  --claim \
  --mark-prepared \
  --report-failure
```

`run-moments` 会把单条朋友圈任务 JSON 写入 handler 的 stdin，并设置 `WECOM_BRIDGE_MOMENT_TASK_ID`、`WECOM_BRIDGE_MOMENT_TEXT`、`WECOM_BRIDGE_MOMENT_MATERIALS` 等环境变量。自带朋友圈 handler 会回传当前企业微信窗口快照，供 Web 管理页确认本机准备环境；默认建议只传 `--mark-prepared`；人工确认已经发布后，再单独执行 `mark-moment-published <taskId>` 回写发布状态。

仓库也提供了一个 AppleScript 包装的企业微信 Mac handler 模板：

```bash
# 只解析 Bridge 回复，不碰企业微信窗口
WECOM_HANDLER_MODE=dry-run \
WECOM_MATERIAL_MAP='{"demo-poster":"/Users/andy/Pictures/material.png"}' \
  scripts/wecom-mac-reply-handler.sh < doc/examples/wecom-bridge-reply.sample.json

# 默认 prepare：打开企业微信、搜索会话、把顺序回复合并粘贴到输入框，不按回车
WECOM_HANDLER_MODE=prepare \
WECOM_APP_NAME='企业微信' \
WECOM_SEARCH_SHORTCUT='command+k' \
WECOM_MATERIAL_MAP_FILE=/Users/andy/.config/wecom-materials.json \
  node scripts/wecom-bridge-client.mjs run-approved \
    --handler './scripts/wecom-mac-reply-handler.sh' \
    --claim \
    --report-failure

# 真正发送必须双重确认：mode=send 且 WECOM_ALLOW_SEND=1；如有 replySteps，会按 text/wait/image/text 顺序执行
WECOM_HANDLER_MODE=send \
WECOM_ALLOW_SEND=1 \
  node scripts/wecom-bridge-client.mjs run-approved \
    --handler './scripts/wecom-mac-reply-handler.sh' \
    --claim \
    --mark-delivered \
    --report-failure
```

这个 handler 需要 macOS「系统设置 → 隐私与安全性 → 辅助功能」允许终端或运行器控制电脑。`WECOM_SEARCH_SHORTCUT=none` 时不会搜索会话，只会把回复粘贴到当前打开的会话输入框，适合先手动定位窗口再联调。`prepare` 模式会把多段文本合并成一段草稿，并把图片粘贴为待确认素材，避免误发；`send` 模式才会尊重 `wait` 步骤逐段发送文本和图片。默认 `WECOM_VERIFY_TARGET=1` 会在操作后读取企业微信窗口标题并回传目标匹配结果；设置 `WECOM_REQUIRE_TARGET_MATCH=1` 时会把这层标题匹配提升为发送前硬门禁。它是窗口/OCR 校验链路的第一层，后续可把真实 OCR 文本继续填到同一个 `verification.ocrText` 字段。

群发任务的 Mac handler 模板：

```bash
# 只解析群发任务，不碰企业微信窗口
WECOM_HANDLER_MODE=dry-run \
  scripts/wecom-mac-mass-handler.sh <<'JSON'
{"id":"job:item","jobId":"job","itemId":"item","jobTitle":"测试队列","recipientName":"测试群","message":"测试内容"}
JSON

# prepare：搜索目标会话并粘贴群发内容，不自动发送
WECOM_HANDLER_MODE=prepare \
WECOM_APP_NAME='企业微信' \
WECOM_SEARCH_SHORTCUT='command+k' \
  node scripts/wecom-bridge-client.mjs run-mass \
    --handler './scripts/wecom-mac-mass-handler.sh' \
    --claim \
    --report-failure

# 真正发送必须双重确认：mode=send 且 WECOM_ALLOW_SEND=1
WECOM_HANDLER_MODE=send \
WECOM_ALLOW_SEND=1 \
  node scripts/wecom-bridge-client.mjs run-mass \
    --handler './scripts/wecom-mac-mass-handler.sh' \
    --claim \
    --mark-sent \
    --report-failure
```

朋友圈任务的 Mac handler 模板：

```bash
# 只解析朋友圈任务，不碰企业微信窗口
WECOM_HANDLER_MODE=dry-run \
  scripts/wecom-mac-moment-handler.sh <<'JSON'
{"id":"draft","draftId":"draft","title":"测试朋友圈","text":"测试朋友圈正文","imageNotes":"无需配图","materials":[]}
JSON

# prepare 默认 clipboard-only：把正文放入剪贴板，不自动发布
WECOM_HANDLER_MODE=prepare \
WECOM_MOMENT_PASTE_MODE=clipboard-only \
WECOM_MATERIAL_MAP_FILE=/Users/andy/.config/wechat-on-cloud/wecom-materials.json \
  node scripts/wecom-bridge-client.mjs run-moments \
    --handler './scripts/wecom-mac-moment-handler.sh' \
    --claim \
    --mark-prepared \
    --report-failure

# 如果已经手动打开朋友圈发布框并定位光标，可改为 current-input 自动粘贴
WECOM_HANDLER_MODE=prepare \
WECOM_MOMENT_PASTE_MODE=current-input \
WECOM_MATERIAL_MAP_FILE=/Users/andy/.config/wechat-on-cloud/wecom-materials.json \
  node scripts/wecom-bridge-client.mjs run-moments \
    --handler './scripts/wecom-mac-moment-handler.sh' \
    --claim \
    --mark-prepared \
    --report-failure
```

朋友圈 handler 会读取任务里的 `materials`，并通过 `WECOM_MATERIAL_MAP` 或 `WECOM_MATERIAL_MAP_FILE` 把云端素材 key 解析为 Mac 本机路径。`dry-run` 会输出 `materialCount`、`resolvedMaterialCount`、`missingMaterialRefs` 和 `missingMaterialPaths`；`prepare` 会先检查素材映射，缺少 key 或本机文件不存在时直接失败并可由 `--report-failure` 回写，不会继续打开企业微信或粘贴正文。

### Mac 定时运行

如果要让 Mac 端定时检查云端批准回复、群发任务和朋友圈任务，可以用 runner：

Web 管理页的「自动化工作台 → 企微接入资料 → Mac Runner 接入」会根据当前访问域名生成 `~/.config/wechat-on-cloud/wecom-bridge.env` 模板和常用运行命令。R79 起还会生成完整 bootstrap 脚本：在新 Mac 上拉取 `andy/automation-lab` 分支、写入本机 env 模板、提示后续 doctor/run-once 命令。R80 起补齐 LaunchAgent 定时运行闭环：面板可复制脱敏 dry-run、实际安装、状态查看、日志跟踪和停用命令；安装脚本会先读取 bootstrap 写好的本机 env 文件，所以不用把 Bridge token 再放到命令行里。R81 起接入本机 `@wecom/cli` 探测：配置模板会写入 `WECOM_CLI_EXECUTABLE`，面板可复制安装、初始化和授权检查命令，`doctor` 会检查 CLI 安装与授权状态。R82 起面板还会给出 `sync-cli-audience` 命令，把已授权企微 CLI 可见的通讯录导入云端受众资产池；默认导入为未审核联系人，仍需管理员筛选/审核后才能进入受控群发。R83 起自动化工作台顶部新增健康度卡，把总览、预检和下一步队列聚合成 0-100 分，并按 AI 回复、群发、朋友圈、Mac Runner、素材受众拆出阻断原因和下一步动作。模板只包含 token 占位符，不会把 NAS 容器里的真实 `AUTOMATION_BRIDGE_TOKEN` 回显到浏览器；真实 token 仍从飞牛 NAS 的 Docker 环境变量里复制。

```bash
export WOC_PANEL_URL='http://nasbot.cloud:36081'
export AUTOMATION_BRIDGE_TOKEN='至少16位随机字符串'

# 安全默认：只列出待处理回复，不领取、不碰窗口
WECOM_RUNNER_MODE=dry-run scripts/wecom-bridge-runner.sh run-once

# 群发队列 dry-run：只列出待处理群发任务
WECOM_RUNNER_MODE=dry-run \
WECOM_RUNNER_TARGET=mass \
scripts/wecom-bridge-runner.sh run-once

# 朋友圈 dry-run：只列出待处理朋友圈任务
WECOM_RUNNER_MODE=dry-run \
WECOM_RUNNER_TARGET=moments \
scripts/wecom-bridge-runner.sh run-once

# 全队列 dry-run：一次汇总回复、群发和朋友圈三类待办
WECOM_RUNNER_MODE=dry-run \
WECOM_RUNNER_TARGET=all \
scripts/wecom-bridge-runner.sh run-once

# 拉取云端 Runner 策略并只打印 JSON
node scripts/wecom-bridge-client.mjs runner-policy --worker-id mac-mini-01

# 安装并初始化企业微信开放能力 CLI，用于复用企微 AI 助手里的通讯录/消息等能力
npm install -g @wecom/cli
wecom-cli init
wecom-cli --version && wecom-cli auth show --auth-status

# 将企微 CLI 通讯录同步进云端受众资产池；默认未审核，不会自动群发
node scripts/wecom-bridge-client.mjs sync-cli-audience --wecom-cli wecom-cli --source wecom-cli-contact --tag wecom-cli

# 接入体检：检查本机脚本、配置文件权限、Bridge token、云端 Runner 策略、素材映射摘要、wecom-cli 和企业微信窗口
scripts/wecom-bridge-runner.sh doctor

# 接入体检并回传到云端最近运行报告
WECOM_DOCTOR_REPORT=1 \
scripts/wecom-bridge-runner.sh doctor

# CI 或只想验证远端鉴权时，可跳过真实企业微信窗口或 wecom-cli 检查
WECOM_DOCTOR_APP=0 \
WECOM_DOCTOR_CLI=0 \
scripts/wecom-bridge-runner.sh doctor

# 使用云端策略：面板控制 mode/target/limit/TTL/心跳间隔
WECOM_USE_REMOTE_POLICY=1 \
WECOM_BRIDGE_WORKER_ID='mac-mini-01' \
scripts/wecom-bridge-runner.sh run-once

# prepare：领取回复并粘贴到企业微信输入框，不自动发送
WECOM_RUNNER_MODE=prepare \
WECOM_BRIDGE_WORKER_ID='mac-mini-01' \
WECOM_CLAIM_TTL_SECONDS=300 \
scripts/wecom-bridge-runner.sh run-once

# prepare 群发：领取群发任务并粘贴到目标会话，不自动发送
WECOM_RUNNER_MODE=prepare \
WECOM_RUNNER_TARGET=mass \
WECOM_BRIDGE_WORKER_ID='mac-mini-01' \
WECOM_CLAIM_TTL_SECONDS=300 \
scripts/wecom-bridge-runner.sh run-once

# prepare 朋友圈：领取草稿并复制到剪贴板，或 current-input 模式粘贴到发布框
WECOM_RUNNER_MODE=prepare \
WECOM_RUNNER_TARGET=moments \
WECOM_MOMENT_PASTE_MODE=clipboard-only \
WECOM_BRIDGE_WORKER_ID='mac-mini-01' \
WECOM_CLAIM_TTL_SECONDS=300 \
scripts/wecom-bridge-runner.sh run-once

# prepare 全队列：按回复、群发、朋友圈顺序处理；朋友圈只准备到剪贴板或发布框
WECOM_RUNNER_MODE=prepare \
WECOM_RUNNER_TARGET=all \
WECOM_MOMENT_PASTE_MODE=clipboard-only \
WECOM_BRIDGE_WORKER_ID='mac-mini-01' \
WECOM_CLAIM_TTL_SECONDS=300 \
scripts/wecom-bridge-runner.sh run-once

# send：领取、粘贴、按回车发送，并回写 delivered
WECOM_RUNNER_MODE=send \
WECOM_ALLOW_SEND=1 \
WECOM_BRIDGE_WORKER_ID='mac-mini-01' \
WECOM_CLAIM_TTL_SECONDS=300 \
scripts/wecom-bridge-runner.sh run-once

# send 全队列：回复和群发会发送；朋友圈仍跳过发布，只保留人工确认流程
WECOM_RUNNER_MODE=send \
WECOM_RUNNER_TARGET=all \
WECOM_ALLOW_SEND=1 \
WECOM_BRIDGE_WORKER_ID='mac-mini-01' \
WECOM_CLAIM_TTL_SECONDS=300 \
scripts/wecom-bridge-runner.sh run-once
```

`doctor` 只做接入体检，不会领取回复、群发或朋友圈任务，也不会触碰企业微信输入框。它会检查 `node`、`osascript`、LaunchAgent 环境、runner/handler 文件、配置文件权限、素材映射文件、Bridge token、远端 Runner 策略和 `wecom-cli` 安装/授权；素材映射可读时会显示已映射数量、跳过数量和素材类型，便于提前发现云端素材缺本机 `localPath`。默认还会用 AppleScript 读取企业微信进程和窗口标题，用于提前发现 macOS 自动化/辅助功能权限问题。设置 `WECOM_DOCTOR_REMOTE=0` 可跳过远端策略鉴权，设置 `WECOM_DOCTOR_APP=0` 可跳过真实企业微信窗口检查，设置 `WECOM_DOCTOR_CLI=0` 可跳过企微 CLI 检查。设置 `WECOM_DOCTOR_REPORT=1` 后，体检结果会以 `target=doctor` 回传到云端 `run-report`，Web 管理页「最近运行」会显示“接入体检”和每项 OK/WARN/FAIL 明细。

runner 每次运行都会先上报一次 `heartbeat`，即使没有待处理回复、群发任务或朋友圈任务，Web 管理页也能看到 Mac 端最近是否在线；runner 调用 `run-approved`、`run-mass`、`run-moments` 时会带 `--report-run` 自动回写本轮运行报告。prepare 模式会 `claim` 当前任务，因此不会重复出现在待处理列表里；人工确认发送后，应再执行 `mark-delivered <eventId>` 或 `mark-mass-sent <taskId>`；人工确认朋友圈已经发布后，执行 `mark-moment-published <taskId>`。`WECOM_RUNNER_TARGET=all` 会按 replies → mass → moments 顺序运行；真实无人值守发送只建议在小号/测试群充分验证后启用 `send`；朋友圈 target 当前不支持 runner 的 `send` 模式，all+send 也会跳过朋友圈发布。启用 `WECOM_USE_REMOTE_POLICY=1` 后，本机每次运行会先向面板拉取云端 Runner 策略；如果云端把 `runnerEngine` 设为 `rpa-package`，本机会自动切到 `run-cloud-rpa-package`，统一通过 RPA 包执行 AI 回复、群发和朋友圈半自动任务；如果云端允许 `send`，本机仍需设置 `WECOM_ACCEPT_REMOTE_SEND=1` 才会接受远程真实发送授权，否则自动降级为 `prepare`；如果云端开启 `requireTargetMatch`，本机会自动设置 `WECOM_REQUIRE_TARGET_MATCH=1`；如果开启 `requireHandlerVerification`，本机会自动设置 `WECOM_REQUIRE_HANDLER_VERIFICATION=1`，要求 handler 回传正向校验后才回写成功状态。回复任务如果带 `replySteps`，runner 会把步骤原样交给 Mac handler；dry-run 输出会显示 `stepCount`、`imageStepCount`、缺失图片路径和未映射素材 key，便于先检查队列而不碰企业微信窗口。prepare/send 输出会带窗口标题校验快照，Web 管理页最近运行报告可直接看到“校验通过/失败”和命中的窗口名；本机设置 `WECOM_REQUIRE_TARGET_MATCH=1` 时，回复和群发会在粘贴前硬拦截目标不匹配。

也可以安装 macOS 用户级 LaunchAgent 定时运行：

```bash
WOC_PANEL_URL='http://nasbot.cloud:36081' \
AUTOMATION_BRIDGE_TOKEN='至少16位随机字符串' \
WECOM_USE_REMOTE_POLICY='1' \
WECOM_RUNNER_MODE='dry-run' \
WECOM_RUNNER_TARGET='all' \
WECOM_BRIDGE_INTERVAL_SEC=60 \
scripts/install-wecom-bridge-launchagent.sh
```

安装脚本会生成：

- 配置文件：`~/.config/wechat-on-cloud/wecom-bridge.env`，权限 `600`，包含 Bridge token。
- LaunchAgent：`~/Library/LaunchAgents/com.wechatoncloud.wecom-bridge.plist`。
- 日志：`~/Library/Logs/com.wechatoncloud.wecom-bridge.log` 和 `.err.log`。

先用 `--dry-run --redact-secrets` 查看将写入的配置和 plist，不会落盘，也不会把本机 env 文件里的 token 打到终端：

```bash
scripts/install-wecom-bridge-launchagent.sh --dry-run --redact-secrets
```

### 模拟命中

```http
POST /api/admin/automation/simulate
Content-Type: application/json

{ "inboundText": "你好，我想咨询一下价格" }
```

返回是否命中规则、风险级别、是否允许进入确认发送。

### 生成回复计划

```http
POST /api/admin/automation/reply-plan
Content-Type: application/json

{
  "inboundText": "你们这个适合什么样的人？",
  "conversationContext": "可选的最近上下文",
  "extraInstruction": "可选的临时指令"
}
```

返回 `keyword-rule`、`ai-draft`、`manual-review` 或 `blocked`，用于管理页「AI 回复」工作台。

### 读取实例剪贴板

```http
POST /api/admin/instances/:id/automation/read-clipboard
Content-Type: application/json

{ "copySelection": true }
```

- `copySelection=true`：先在实例里模拟 `Ctrl+C`，再读取剪贴板，适合远程微信里已经选中一段客户消息的场景。
- `copySelection=false`：直接读取实例当前剪贴板。
- 返回文本只进入工作台输入框，不会自动发送。

### 生成 AI 草稿

```http
POST /api/admin/automation/ai-draft
Content-Type: application/json

{
  "inboundText": "你们这个适合什么样的人？",
  "conversationContext": "可选的最近上下文",
  "extraInstruction": "可选的临时指令"
}
```

### 确认发送命中规则

```http
POST /api/admin/instances/:id/automation/send-rule
Content-Type: application/json

{
  "ruleId": "welcome-001",
  "inboundText": "你好，我想咨询一下价格",
  "conversationName": "张三",
  "confirm": true
}
```

### 确认发送单条文本

```http
POST /api/admin/instances/:id/automation/send-text
Content-Type: application/json

{
  "text": "这是人工确认后的回复",
  "inboundText": "客户刚发来的消息",
  "conversationName": "张三",
  "confirm": true,
  "allowReview": false
}
```

### 群发队列

创建队列：

```http
POST /api/admin/automation/mass-jobs
Content-Type: application/json

{
  "title": "6 月活动提醒",
  "message": "这是一条经过确认的群发内容",
  "recipients": ["客户群 A", "张三"],
  "options": {
    "perSendDelaySeconds": 10,
    "requireOperatorConfirmRecipient": true,
    "openConversationBeforeSend": true,
    "searchShortcut": "ctrl+f",
    "searchResultDelaySeconds": 2,
    "postOpenDelaySeconds": 1
  }
}
```

也可以从已导入的受众资产直接生成队列；默认只选入已启用、已审核的受众，生成后仍是独立的未审核队列，不会绕过审核、限流和 Bridge 领取机制：

```http
POST /api/admin/automation/mass-jobs/from-audience
Content-Type: application/json

{
  "title": "活动提醒 - 白名单客户群",
  "message": "这是一条经过确认的群发内容",
  "audienceFilter": {
    "tag": "活动提醒",
    "type": "group",
    "limit": 100
  },
  "options": {
    "perSendDelaySeconds": 10,
    "requireOperatorConfirmRecipient": true,
    "openConversationBeforeSend": true
  }
}
```

审核/暂停/取消队列：

```http
PATCH /api/admin/automation/mass-jobs/:jobId
Content-Type: application/json

{ "approved": true, "status": "queued" }
```

发送下一条：

```http
POST /api/admin/instances/:id/automation/mass-jobs/:jobId/send-next
Content-Type: application/json

{
  "confirm": true,
  "operatorConfirmedRecipient": true,
  "openConversationBeforeSend": true
}
```

注意：

- `openConversationBeforeSend=true` 时，面板会尝试用搜索快捷键打开目标会话，再粘贴发送内容。
- 如果某个微信版本快捷键不一致，可把队列改成 `openConversationBeforeSend=false`，回到“人工打开当前会话后发送”的受控模式。
- 无论哪种模式，每次发送仍要求 `confirm=true` 和 `operatorConfirmedRecipient=true`。
- 如果发送失败，队列会自动暂停，当前目标标记为 `failed`，管理页可把目标重置为 `pending` 后重试，也可以跳过继续跑后续目标。

更新单个队列目标状态：

```http
PATCH /api/admin/automation/mass-jobs/:jobId/items/:itemId
Content-Type: application/json

{ "status": "skipped", "reason": "本次不发送" }
```

### 朋友圈草稿

AI 生成文案：

```http
POST /api/admin/automation/moment-drafts/ai-draft
Content-Type: application/json

{
  "topic": "本周 AI 自动化实践复盘",
  "audience": "私域客户和学员",
  "tone": "自然、克制、有个人感"
}
```

保存草稿：

```http
POST /api/admin/automation/moment-drafts
Content-Type: application/json

{
  "title": "AI 自动化复盘",
  "text": "朋友圈正文",
  "imageNotes": "配 3 张操作截图",
  "materials": ["screenshot-1.png", "screenshot-2.png"]
}
```

填入当前朋友圈发布框：

```http
POST /api/admin/instances/:id/automation/moment-drafts/:draftId/prepare
Content-Type: application/json

{ "confirm": true, "mode": "copy-to-clipboard" }
```

`mode` 可选：

- `copy-to-clipboard`：只复制到实例剪贴板，用户自己在朋友圈发布框里粘贴。
- `fill-current-input`：直接粘贴到当前焦点输入框，适合已经打开朋友圈发布框并定位好光标的场景。

图片选择、发布前检查和点击发布仍由人工完成。

## 和 wecom-ai-assistant 的融合关系

已融合的部分：

- `ReplyPipeline` 思路：先关键词规则，再知识库/AI，再人工处理。
- 企微自动化 Mac 版资料接入：FAQ、话术、边界、人群分组和朋友圈素材可以导入为结构化资料。
- 受众资产池：联系人、客户群、群聊、标签和来源可长期沉淀，并一键填入群发目标或按筛选直接生成受控群发队列。
- 企微 CLI 通讯录同步：Mac 端可通过 `@wecom/cli contact get_userlist` 把可见通讯录标准化导入受众资产池，默认未审核、只作为群发候选资产。
- 素材资产台账：`imageKey/materialKey` 可在云端登记、审核、启停和删除，Mac 端按本机映射解析真实文件。
- Bridge 素材映射：Mac Runner 可用 token 拉取已审核素材，按 kind/tag/source 生成 `WECOM_MATERIAL_MAP_FILE`，运行前自动刷新，在 doctor 中展示 mapped/skipped 摘要，并通过 heartbeat 把本机映射状态显示到 Web worker 列表和预检报告，减少手工维护 key/path JSON。
- 自动化资产包：规则、接入资料、受众、素材、群发队列和朋友圈草稿可导出、dry-run 预览并导入到另一套测试面板。
- Mac Bridge：企微 Mac 工具可用 token 把资料推送到云端面板，不必手工复制粘贴。
- Bridge 消息收件箱：企微 Mac 工具可把新消息推到面板，可由管理员一键生成并保存回复草稿，也可在推送时用 `--plan-replies` 自动生成待审草稿。
- Bridge 回复出箱：管理员批准后的回复草稿或顺序回复 `replySteps` 可供 Mac 工具拉取执行，云端仍不直接发送。
- Bridge 朋友圈出箱：管理员审核后的朋友圈草稿可供 Mac 工具领取并准备，人工发布后回写状态。
- 朋友圈素材预检：Mac handler 可按云端素材 key 解析本机文件，缺失素材会在半自动 prepare 前失败回写。
- Bridge 运行明细：Mac Runner 每轮可回写结构化 `items`，管理页最近运行报告能看到具体任务、动作和失败原因。
- Bridge 目标校验快照：Mac/RPA handler 可把目标会话匹配、窗口标题、OCR 文本、输入框就绪状态随运行报告回传，云端管理页可见。
- RPA 包签发台账：Web 签发和 Mac 回执可按 `packageDigest/taskDigest` 关联，便于追踪哪份包被哪台 Mac 执行。
- Bridge 出箱恢复：管理员可在管理页预览、限定队列范围、worker、失败原因、失败冷却和重试次数上限，分批释放超时领取，并把失败的回复、群发目标和朋友圈草稿重新放回待处理队列。
- Mac Runner 接入体检：本机可用 `scripts/wecom-bridge-runner.sh doctor` 检查脚本、配置权限、Bridge token、云端策略、素材映射、企微 CLI 授权和企业微信窗口权限；设置 `WECOM_DOCTOR_REPORT=1` 后会把体检结果回传到云端最近运行报告，上线前先排除环境问题。
- 实例感知快照：Web 工作台可以直接读取运行实例当前活动窗口、焦点窗口、可见窗口、指针位置和可选截图；当前是只读观测层，后续 OCR/视觉识别会接在同一条链路上。
- AI 回复和朋友圈文案会自动引用已审核接入资料；人群分组可在管理页填入群发目标。
- `RiskClassifier` 思路：阻断高风险词，普通词放行，部分词进入复核。
- `KeywordReplyStep` 思路：多段文本、图片素材路径与延迟步骤已经可由云端保存、Bridge 拉取，并由 Mac handler 在 prepare/send 模式按序处理。
- `AutomaticReplyPolicy` 思路：已审核规则、冷却时间、每小时上限。
- 自动化预检：在 Mac Runner 领取前检查开关、心跳、目标队列、规则、素材和风控阻断。
- 审计和人工确认优先的产品边界。
- 管理页工作台：AI 回复、群发队列、朋友圈草稿和最近审计记录集中在一个入口。
- Docker 执行器：会话搜索打开、实例剪贴板读写、自动化环境自检。
- 群发队列恢复能力：失败自动暂停，目标可跳过、重置或标记失败。

尚未融合的部分：

- Mac 版工具已经可以通过 Bridge 领取 AI 回复、群发任务和朋友圈任务，并由本机 handler 搜索会话、准备文案、粘贴或发送；云端已支持接收目标会话/OCR/窗口校验快照，回复和群发 handler 已支持 `WECOM_REQUIRE_TARGET_MATCH=1` 的窗口标题硬门禁，Bridge client 也支持 `WECOM_REQUIRE_HANDLER_VERIFICATION=1` 的交付成功门禁，但真实 OCR 与视觉识别 handler 仍需接入。
- 读取微信聊天消息：当前版本还没有可靠 OCR/窗口识别。
- 联系人/群匹配校验：云端已有回传和展示通道，当前还缺 Mac 端 OCR/视觉识别来稳定反查“是否确实打开了目标会话”。
- 朋友圈发布状态机：当前 Bridge 已支持半自动领取、素材映射预检、正文准备和状态回写，未实现自动打开入口、选图和点击发布的 RPA 流程。

## 下一步

1. 增强实例内感知：在现有活动窗口/可见窗口/截图快照基础上接入 OCR、输入框定位和视觉摘要。
2. 增强联系人/群匹配校验：当前已有窗口标题硬门禁和实例感知快照，下一步接入 OCR/视觉识别反查“确实打开了目标会话”。
3. 增强朋友圈 RPA：自动打开朋友圈入口、进入发布框、选图、填文案、停在发布确认前。
4. 增加按队列策略恢复；群发 Bridge 当前按单队列顺序领取。
5. 小范围测试稳定后，再考虑构建自定义镜像并部署到 NAS 的单独测试端口。
